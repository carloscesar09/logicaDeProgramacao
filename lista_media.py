# inicio
lista_media = ["7", "8", "9", "6", "8", "8" ]
def funcao_4 ( ): 
  lista_media = [ ]
  i = 0 
  while i < rangee ( len (lista))
  i += 1
  elemento = lista [i]
  for elemento = lista:
   if elemento >= "7"
     lista_media += 1
   if lista_media / len > 0.75
     print ("Professor padrão")
   else:
     print( "Professor coxa")
# fim
